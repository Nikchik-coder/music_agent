# General App Settings
YOUTUBE_ENABLED=True
AGENT_VOICE_SYSTEM_PATH=

# API
GOOGLE_API_KEY=
TOGETHER_API_KEY=
CARTESIA_API_KEY=
APIFY_TOKEN=

#Elevenlabs configuration
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
ELEVENLABS_MODEL_ID=

# OBS
OBS_HOST=192.168.0.211
OBS_PORT=4455
OBS_PASSWORD=

# Logging Configuration
LOG_LEVEL=ERROR  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

#Agent configuration (example: lumira.json.template)
AGENT_PERSONALITY_PATH=

# YouTube
api_key=
voice_id=
VOICE_FEATURE=False


# LLM Configuration
MODEL_PROVIDER=google
MODEL_NAME=gemini-2.5-flash

# Validation LLM Configuration
MODEL_VALIDATION_PROVIDER=together
MODEL_VALIDATION_NAME=meta-llama/Llama-3.3-70B-Instruct-Turbo

# Space LLM
MODEL_PROVIDER_SPARE=together
MODEL_NAME_SPARE=deepseek-ai/DeepSeek-V3

# Audio Configuration
BACKGROUND_MUSIC_VOLUME_NORMAL=0.3
BACKGROUND_MUSIC_VOLUME_DUCKED=0.1

# YOUTUBE INTERFACE
TOGETHER_API_KEY=
STREAM_ID=
GOOGLE_API_KEY=

# Paths relative to this folder or absolute:
CLIENT_SECRET_FILE_PATH="./files/client_secret.json"
TOKEN_FILE_PATH="./files/token.json"
MEMORY_FILE_PATH="./files/memory.json"

# --- YouTube OAuth 2.0 credentials ---
# TODO: take this just from the client_secrets.json
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REFRESH_TOKEN=

# --- Media Path Configuration for Docker Volume Mapping ---
# Host-side absolute directory that should contain all media we want accessible outside Docker
MEDIA_HOST_DIR=/Users/wotori/xyber_media

# Container-side mount point for MEDIA_HOST_DIR (fixed to /app/media)
MEDIA_CONTAINER_DIR=/app/media

# Voice (host) path OBS should read from
AGENT_VOICE_PATH=${MEDIA_HOST_DIR}/voice/generated_audio

# Where TTS should write files INSIDE the container
VOICE_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/voice/generated_audio

# Where SoundCloud (and other music) should be saved INSIDE the container
SOUNDCLOUD_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/music/soundcloud_songs
GOOGLE_DRIVE_MUSIC_DIR=${MEDIA_CONTAINER_DIR}/music/google_drive_songs

# News and other media directories
NEWS_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/news
STATE_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/state
MEMORY_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/memory
VIDEOS_OUTPUT_DIR=${MEDIA_CONTAINER_DIR}/videos


#MCP Instruments for the agent
MCP_TAVILY_URL=
MCP_ARXIV_URL=
APIFY_TOKEN=
MCP_APIFY_URL =
MCP_TELEGRAM_PARSER_URL=

